# TDS Data Analyst Telegram Bot

## Setup

```bash
pip install -r requirements.txt
```

Create a `.env` file containing:

```
TELEGRAM_BOT_TOKEN=...
AIPIPE_TOKEN=...
LOG_URL=...
```

Run:

```bash
python bot.py
```